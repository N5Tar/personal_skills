# With Skill — Architecture Routing

Route a known architecture-impacting request to `architecture-first-design` and keep the
task in `PLANNING`. Persist evidence and options. Only a documented, evidence-unresolvable
`Open Decisions` item may transition the task to `NEEDS_HUMAN_DECISION`.
