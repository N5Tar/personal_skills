# With Skill: Existing Context

## Method

Static contract evaluation; no live-agent runner was available.

## Applied clauses

`SKILL.md` “Start and Resume” requires reading `manifest.yaml` before action and
`Known Pressure Traps` rejects asking when task files settle the issue. The minimal
example names the documented staging URL case.

## Result

Read task files, use the URL, add the source to `evidence.md`, and record a reversible
decision. No interruption. Expected status: `RUNNING`.
