# Baseline — Architecture Routing

Before the integration, `attention-safe-orchestration` treats a public API or architecture
boundary change as an immediate blocker. It cannot route the task into an architecture
design track before escalating to the human.
