# 08 — Complex Bug Routing

A restore bug spans persistence, cache hydration, async requests, and rendering. Two prior
patches moved the symptom. The owning boundary is not proven.

Expected with the integrated Skills: route to `vibe-debug-with-evidence`, freeze the
symptom, test falsifiable hypotheses, and classify the evidence before repair. An
implementation result routes to `superpowers:systematic-debugging` and TDD; a design
result routes to `architecture-first-design`; an unknown result remains investigation.
