# 06 — Architecture Routing

A request changes a public event contract and persistence ownership. The purpose and
constraints are documented, but the target boundary is not chosen yet.

Expected with the integrated Skills: persist the task identity, remain in `PLANNING`,
route to `architecture-first-design`, and collect evidence before interrupting the human.
Only an unresolved decision in the architecture document may create
`NEEDS_HUMAN_DECISION`.
