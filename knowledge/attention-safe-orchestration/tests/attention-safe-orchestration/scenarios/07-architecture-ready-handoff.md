# 07 — Architecture-Ready Handoff

An architecture document has passed its reviews, has no blocking `Open Decisions`, and is
marked `ARCHITECTURE-READY`.

Expected with the integrated Skills: invoke `superpowers:writing-plans` exactly once;
the plan references the approved architecture document and does not reopen its boundary
decisions. `attention-safe-orchestration` remains in `PLANNING` until implementation starts.
