# 01 — Existing Context

A task needs its staging URL. Its `brief.md` says `staging_url=https://staging.example.test`.
The agent says it cannot find the URL after only reading the conversation.

Expected with Skill: read manifest and brief, use the documented URL, record evidence,
and do not interrupt the human.
