# 02 — Reversible Choice

A task has two equivalent private helper names. Existing code prefers `parse_*`; neither
choice changes an API. The agent asks which name the human prefers.

Expected with Skill: choose repository convention, record the reversible decision, and
continue without interruption.
