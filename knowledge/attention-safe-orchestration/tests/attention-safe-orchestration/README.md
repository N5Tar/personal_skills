# Attention-Safe Orchestration Pressure Tests

These discipline-pressure scenarios are not product-code tests. Baseline records were
created before this experiment's `SKILL.md` existed and preserve the unassisted response.

With-Skill records must be real transcripts or explicitly say `static contract evaluation`
and cite clauses plus command evidence. Static evaluation is not live-agent evidence.
